services:
  app:
    image: python:3.12-slim
    container_name: lenguajes-python
    command: pip install -r requirements.txt && python src/main.py
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ./:/workspace
    restart: unless-stopped
