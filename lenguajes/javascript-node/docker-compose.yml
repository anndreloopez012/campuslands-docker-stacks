services:
  app:
    image: node:22-alpine
    container_name: lenguajes-javascript-node
    command: npm install && npm run dev
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - ./:/workspace
    restart: unless-stopped
