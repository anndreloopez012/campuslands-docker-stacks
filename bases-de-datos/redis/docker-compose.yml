services:
  app:
    image: redis:7-alpine
    container_name: bases-de-datos-redis
    ports:
      - "${REDIS_PORT:-6380}:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
